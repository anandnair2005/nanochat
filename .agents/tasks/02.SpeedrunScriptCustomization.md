# Task 02: Implement Vast Speedrun Script

## Status
Completed.

## Completion Notes
- Implemented `runs/speedrun_vast.sh`.
- Verified with `bash -n runs/speedrun_vast.sh`.
- Verified missing prewarmed data fails early with `NANOCHAT_SKIP_UV_SYNC=1`, `NANOCHAT_SKIP_DATASET=1`, and `NANOCHAT_SKIP_TOKENIZER=1`.
- Verified failure path writes `status/speedrun.FAILED` and `logs/speedrun.log`.
- Verified default H100 commands preserve `--nproc_per_node=8`, `--depth=24`, `--target-param-data-ratio=8`, `--device-batch-size=16`, `--fp8`, base eval, SFT, and chat eval commands.
- Revised behavior: base training now uses `--save-every=${NANOCHAT_BASE_SAVE_EVERY:-200}` for crash resilience.
- Revised behavior: if restored base checkpoints exist under `base_checkpoints/${NANOCHAT_BASE_MODEL_TAG:-d24}`, the wrapper auto-detects the latest step and passes `--resume-from-step`.
- Revised behavior: when `NANOCHAT_ENABLE_GDRIVE_SYNC=1`, the wrapper starts background artifact sync and runs a blocking final sync.
- Verified revised missing prewarmed data failure path after adding resume/sync hooks.
- Full training was not run locally because it requires an 8xH100 CUDA environment.

## Agent Objective
Create a Vast-specific speedrun script that preserves the default H100 training parameters from `runs/speedrun.sh` while allowing prewarmed storage and baked Docker dependencies to skip setup work.

## Files To Create Or Modify
- Create `runs/speedrun_vast.sh`.
- Read `runs/speedrun.sh` before editing to mirror the current upstream flow.
- Do not modify `runs/speedrun.sh` unless there is a minimal bug fix required, and ask before doing so.

## Required Behavior
- Use `#!/usr/bin/env bash` and `set -euo pipefail`.
- Require `NANOCHAT_BASE_DIR` unless `NANOCHAT_ALLOW_DEFAULT_BASE_DIR=1` is supplied.
- Create `$NANOCHAT_BASE_DIR`, `$NANOCHAT_BASE_DIR/logs`, and `$NANOCHAT_BASE_DIR/status`.
- Write the main log to `${NANOCHAT_LOG_FILE:-$NANOCHAT_BASE_DIR/logs/speedrun.log}`.
- Write `speedrun.STARTED`, `speedrun.DONE`, and `speedrun.FAILED` marker files under `$NANOCHAT_BASE_DIR/status`.
- Support `NANOCHAT_SKIP_UV_SYNC=1`.
- Support `NANOCHAT_SKIP_DATASET=1`.
- Support `NANOCHAT_SKIP_TOKENIZER=1`.
- Support `NANOCHAT_RUN_ID`, defaulting to a timestamped value if unset.
- Preserve `WANDB_RUN=dummy` behavior when `WANDB_RUN` is unset.
- Preserve the default speedrun commands and parameters, including `--depth=24`, `--target-param-data-ratio=8`, `--device-batch-size=16`, `--fp8`, and `--nproc_per_node=8`.
- Keep report reset/generation behavior equivalent to `runs/speedrun.sh`.
- Download `identity_conversations.jsonl` unless it already exists or `NANOCHAT_SKIP_IDENTITY_DOWNLOAD=1` is set.
- If `NANOCHAT_SKIP_DATASET=1`, verify `base_data_climbmix` exists and has parquet files before training.
- If `NANOCHAT_SKIP_TOKENIZER=1`, verify tokenizer artifacts exist before training.

## Implementation Constraints
- Keep this script self-contained and shell-based.
- Do not implement Google Drive sync in this task; leave hooks only if simple and documented.
- Do not change default model/training parameters.
- Do not make CPU/MPS behavior part of this script.
- Avoid `cd` inside examples; the script may determine repo root from its own location.

## External Tools Required
- Shell, Python 3.10, PyTorch CUDA environment, and `torchrun`.
- No Docker daemon is required to implement this task.
- No MCP server is required.

## Commands To Run
- `bash -n runs/speedrun_vast.sh`
- `NANOCHAT_BASE_DIR=/tmp/nanochat-speedrun-missing NANOCHAT_SKIP_UV_SYNC=1 NANOCHAT_SKIP_DATASET=1 NANOCHAT_SKIP_TOKENIZER=1 bash runs/speedrun_vast.sh` should fail early because required prewarmed artifacts are missing.
- If a safe dry-run mode is implemented, run it and document the command.

## Acceptance Criteria
- `runs/speedrun_vast.sh` exists and passes shell syntax validation.
- The script preserves the default H100 training/eval/SFT/chat eval command parameters.
- The script can skip setup phases using environment variables.
- The script fails before H100 training if skip flags are set but prewarmed artifacts are missing.
- The script writes clear status markers and logs.
- The final agent response lists changed files, commands run, and any commands not run due to missing GPU hardware.

## Stop And Ask If
- You believe default training parameters must change.
- The user wants the upstream `runs/speedrun.sh` modified directly.
- Verification would require launching an 8xH100 job.
