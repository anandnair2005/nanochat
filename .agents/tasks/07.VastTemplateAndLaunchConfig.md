# Task 07: Implement Vast Launch Configuration Documentation

## Status
Completed.

## Completion Notes
- Created `docs/vast_launch.md`.
- Created non-secret example config `tasks/vast_launch.example.env`.
- Documented the selected split launch mode: H100 uses the custom nanochat entrypoint image, while prewarm uses a smaller Python image with Vast SSH/on-start mode.
- Documented that Vast SSH/Jupyter modes replace or wrap normal image entrypoint behavior and should not be used for H100 unless deliberately testing a different launch path.
- Documented manual H100 creation requirements, including image, volume link, mount path, TCP 22 exposure, public key env var, run env vars, and orchestrator follow-up command.
- Documented prewarm behavior, including automatic create/reuse of at most one 150 GB volume, smaller prewarm image, on-start package install, SSH key attach, repo rsync, rclone injection, and tmux launch.
- Documented storage, secret handling, Google Drive artifact policy, cleanup responsibility, and cheap validation guidance.
- Verified referenced scripts/docs exist. No markdown linter is configured in this repo.

## Agent Objective
Create concrete Vast launch configuration documentation and machine-readable examples that the local orchestrator and user can use to launch prewarm and H100 runs consistently.

## Files To Create Or Modify
- Create `docs/vast_launch.md` or `tasks/vast_launch.md`.
- Create `tasks/vast_launch.example.env` with non-secret example values.
- Update `tasks/vast_orchestrate.example.json` if Task 06 created one.
- Do not add real credentials, rclone configs, wandb keys, Vast API keys, or Docker credentials.

## Required Content
- Document the selected launch mode for H100 runs.
- Document the selected launch mode for prewarm runs.
- Explain that Vast SSH/Jupyter modes replace the Docker entrypoint and require on-start scripts.
- Provide Entrypoint-mode environment variable examples.
- Provide SSH/on-start script examples if SSH mode is supported.
- Document required env vars: `NANOCHAT_BASE_DIR`, `NANOCHAT_AUTORUN`, `NANOCHAT_SKIP_UV_SYNC`, `NANOCHAT_SKIP_DATASET`, `NANOCHAT_SKIP_TOKENIZER`, `NANOCHAT_RUN_ID`, `WANDB_RUN`, and `NANOCHAT_RCLONE_REMOTE`.
- Document secret handling for rclone, wandb, and Vast credentials.
- Document that `WANDB_API_KEY` is supplied by the local SSH orchestrator when starting the training tmux session.
- Document that rclone credentials are copied or injected at runtime by the local orchestrator and are not part of the Docker image.
- Document that H100 selection is manual, while prewarm offer selection may be proposed by the orchestrator and must be confirmed by the user.
- Document storage sizing assumptions and mount path conventions.

## Implementation Constraints
- Keep docs actionable and command-oriented.
- Use placeholders for user-specific values.
- Do not claim automatic destroy-on-exit; explicitly state that the local orchestrator must destroy resources.
- Mention that a cheap Vast test should validate launch mode behavior before H100 use.

## External Tools Required
- No external tools are required to write docs.
- Vast CLI/API docs are useful for accuracy.
- A Vast MCP server would help validate live template fields, but is not required.

## Commands To Run
- If markdown linting exists in the repo, run it. If none exists, state that no markdown lint is configured.
- Verify referenced scripts exist or clearly mark them as produced by earlier tasks.

## Acceptance Criteria
- Launch documentation exists and is specific enough for a user or orchestrator implementation.
- Example env/config file exists and contains no secrets.
- Docs clearly distinguish Entrypoint and SSH/on-start behavior.
- Docs include budget, storage, and secret-handling guidance.
- The final agent response lists any assumptions that still require live Vast validation.

## Stop And Ask If
- The user wants the final launch mode changed from Entrypoint to SSH-only or Jupyter-only.
- Live Vast template creation is required instead of documentation/config examples.
